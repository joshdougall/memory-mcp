{
  "name": "memory-mcp",
  "version": "1.0.0",
  "description": "Valkey-backed MCP memory server for AI agents — persistent, searchable, versioned cross-session memory",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "vitest run",
    "lint": "eslint server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ioredis": "^5.10.1",
    "prom-client": "^15.1.3",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "vitest": "^2.0.0"
  }
}
